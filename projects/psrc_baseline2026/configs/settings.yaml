#----------------------------------------
# settings registered as orca injectables
#----------------------------------------

base_year: 2020
end_year: 2060

calibrated: true

# control total type (regional or subregional)
hh_ct_type: regional
job_ct_type: regional

# orca steps to run during simulation:
preprocessing_steps:
  - load_data
  - build_networks
  - register_variables
  - setup_lcms

simulation_steps:
